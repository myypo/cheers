[package]
name = "realtime"
edition.workspace = true
license.workspace = true
publish = false
version = "0.0.0"

[dependencies]
axum = "0.8"
cheers = { path = "../../cheers" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
