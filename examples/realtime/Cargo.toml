[package]
name = "realtime"
edition.workspace = true
license.workspace = true
publish = false
version = "0.0.0"

[dependencies]
cheers.workspace = true
axum = "0.8"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
rand = "0.8"
